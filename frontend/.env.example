NEXT_PUBLIC_COMVOLY_API_URL=http://localhost:8080

# Isolated development Neon Auth URL only. Leave unset in production until the
# production rollout is separately approved.
NEXT_PUBLIC_NEON_AUTH_URL=

# The account route is static and safe when Auth is absent: it shows an unavailable
# state and does not expose a sign-up form.
